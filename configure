#!/bin/bash

./setup.yml -t user-install -e uid=b1 $*
./setup.yml -t user-config -e uid=b1 $*
# ./setup.yml -t user-admin -e uid=b1 $*
#!/usr/bin/env -S ansible-playbook -i vars/hosts.yml -l localhost
